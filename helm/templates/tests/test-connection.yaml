apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "matrix-service.fullname" . }}-test-connection"
  labels:
    {{- include "matrix-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "matrix-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
