apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-service
spec:
  rules:
    - host: myapp.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: matrix-service
                port:
                  number: 80