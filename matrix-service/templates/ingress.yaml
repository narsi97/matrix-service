apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: matrix-service
                port:
                  number: 80